import React, { PureComponent, Fragment } from "react";import classnames from "classnames";import CheckboxThreeState from "./CheckboxThreeState";import styles from "./index.module.css";// Used together with gridsclass FlexHeader extends PureComponent {  render() {    const {      onChange,      checked, //false - none row checked, any number - some rows checked, true - all rows checked      checkbox,      children,      _scaleRef,      ...otherProps    } = this.props;    const className = classnames(this.props.className, {      [styles.header]: true,      [styles.headerWithCheckbox]: true,      [styles.headerChecked]: checked !== false    });    return (      <div {...otherProps} className={className} ref={_scaleRef}>        {checkbox ? (          <Fragment>            <CheckboxThreeState checked={checked} onChange={onChange} />            {children}          </Fragment>        ) : (          children        )}      </div>    );  }}FlexHeader.defaultProps = {  className: styles.defaultDimensions};export default FlexHeader;